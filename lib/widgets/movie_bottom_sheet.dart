import 'package:flutter/material.dart';

class MovieBottomSheet extends StatelessWidget {
  const MovieBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("hi"),
    );
  }
}
